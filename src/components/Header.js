import Nav from './Nav';

function Header() {
    return (
        <header>
            <img src="/Logo.svg" alt="Little Lemon logo" />
            <Nav />
        </header>
    );
}

export default Header;